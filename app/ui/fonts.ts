import { Nunito_Sans, Quicksand } from 'next/font/google';

export const nunitoSans = Nunito_Sans({ subsets: ['latin'] });
export const quicksand = Quicksand({ subsets: ['latin'], weight: ['500'] });
